---
applyTo: "**/*.test.ts,e2e/**,playwright.config.ts"
---
Keep tests deterministic and ownership-sensitive. Unit tests must not require production credentials; browser tests may use local services only.
